import React from "react";

function App(props) {
  // HOOK : use 로 시작하는 함수
  // hook은 컴포넌트 상단에 작성해야함

  if (true) {
    // hook 호출 코드 작성 불가
  }

  for (let i = 0; i < 3; i++) {
    // hook 호출 코드 작성 불가
  }

  function handle() {
    // hook 호출 코드 작성 불가
  }

  return <div></div>;
}

export default App;
